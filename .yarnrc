# Pin the registry to npm's own, actively maintained registry.
# Yarn Classic's default mirror (registry.yarnpkg.com) has been failing TLS
# validation from CI with "certificate has expired"; registry.npmjs.org does
# not have that problem.
registry "https://registry.npmjs.org"
